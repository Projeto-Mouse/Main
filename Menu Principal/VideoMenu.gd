extends VideoStreamPlayer

# Tem que implementar a funcao de ambos quando 
# configurarmos o menu efeito do menu dependendo da honra, por hora, so gerei o script

# Chamado quando o nó entra na árvore de cena pela primeira vez.
func _ready() -> void:
	pass 

# Chamado de cada quadro. 'delta' é o tempo decorrido desde o quadro anterior.
func _process(delta: float) -> void:
	pass
